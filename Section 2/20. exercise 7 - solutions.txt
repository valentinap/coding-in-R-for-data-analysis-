# 1) save today's date in one object called “today”, using the function base Sys.Date() and the # function of lubridate today()today <- Sys.Date()install.packages('lubridate')library(lubridate)today2 <- today()# 2) convert the string '3-March-2018' with the most suitable functionas.Date('3-March-2018', format = "%d-%b-%y")as.Date("3-March-2018", format = "%d-%b-%y")strptime("3-March-2018", format = "%d-%b-%y")as.POSIXct("March-3-2018 12:20:15", format = "%B-%d-%Y %H:%M:%S")# 3) parse the string "20200131" with the appropriate lubridate function                                            ymd("20200131")# 4) Given the vector x featuring the numbers 75, 98 and 47, # convert it to character with the most suitable function, # then convert it back to numeric format, checking the class # each time x <- c(75, 98, 47)class(x)x <- as.character(x)class(x)x <- as.numeric(x)xclass(x)# 5) convert the first 10 cases of the Orange dataset to a # matrix and then back to a dataframesub1 <- as.matrix(Orange[1:10,])df1 <- as.data.frame(sub1)# 6) install the tidyverse package with the following code and # print the first 5 cases of the iris dataset and the first 5 # cases of the diamonds dataset with the function head()install.packages('tidyverse')library(tidyverse)head(iris, 5)head(diamonds, 5)